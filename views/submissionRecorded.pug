extends layout

block content
    .container
        if id_no
            p Matur skráður á 
            strong= id_no
        else
            p Þú ert þegar skráður í mat
    
        .footer
            a(href='/' class='link') Forsíða