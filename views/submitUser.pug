extends layout

block content
    .container
        if user
            p Notandi skráður
            strong= user.name
            strong= user.idno
        else
            p Notandi er nú þegar til
        
        .footer
            a(href='/' class='link') Forsíða